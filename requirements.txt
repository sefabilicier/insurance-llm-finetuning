# ============================================
# Core ML/DL Dependencies (Python 3.12 Compatible)
# ============================================
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=0.17.0

# Hugging Face Ecosystem
transformers>=4.40.0
datasets>=2.19.0
tokenizers>=0.15.1
peft>=0.11.0
trl>=0.8.0
huggingface-hub>=0.23.0

# Quantization & Optimization
bitsandbytes>=0.43.0
# flash-attn requires specific CUDA version; comment out if issues
# flash-attn>=2.5.6

# Experiment Tracking
wandb>=0.17.0

# Model Serving
# vllm requires specific setup; optional for Phase 2
# vllm>=0.4.0
uvicorn>=0.28.0
fastapi>=0.110.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0

# Evaluation & Metrics
rouge-score>=0.1.2
nltk>=3.8.1
evaluate>=0.4.1

# PII Masking (optional but recommended)
# presidio requires spacy model; install separately if needed
# presidio-analyzer>=2.2.353
# presidio-anonymizer>=2.2.353

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.67.0
click>=8.1.7
loguru>=0.7.2
requests>=2.31.0

# Development (optional)
jupyter>=1.0.0
ipython>=8.21.0
black>=24.1.0
flake8>=7.0.0
mypy>=1.8.0
pytest>=7.4.4

# Data deduplication (optional)
datasketch>=1.0.8



pandas 
numpy 
requests 
pyyaml 
python-dotenv 
datasets 
tqdm